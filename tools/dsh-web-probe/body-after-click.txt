DSH Local Build
b150a55
新会话
任务看板
SSH
技能中心
工作区
milthm-juce
school-net
AgentSoftware
进行中
修复Web界面消息显示
5分钟
AgentSystem与Web的关系
7小时
Implement Web Input Class for TalkToClient
6小时
运行Maven测试并报告问题
22小时
LLM Provider管理器设计与推送
23小时
dsh-mcp-server
img2pdf
AgentCompany
linux-PKGBUILD
snake
认知实习
Blog-Material-You
guochuang_case
wuyitang-local
maf_scheduler
deepseek-harness-plugin
deepseek-harness
workspace
未分组
设置
修复Web界面消息显示
标准模式
Session log
对话
轨迹
这个 Web 界面貌似没有消息，修复一下，改成思维链、工具调用（参数与输出结果）、最终输出都在 Web 界面上显示。
18:26
上下文注入
@deepseek-ai/dsh-system-prompt
失败
Bash
Locate playwright chromium and module
Think
The grep for playwright .bin returned nothing and ls for node_modules/playwright failed — wait, exit code 2 is from the ls. Hmm "ls ~/.cache/ms-playwright/chromium-1234/" shows only chrome-linux64 dir + markers — but the first earlier ls showed "chromium-1234, chromium_headless_shell-1234, ffmpeg-1011" under ~/.cache/ms-playwright (that was listing the parent dir with dirs... wait no, it listed files chromium-1234 etc. as dirs). Now listing chromium-1234 shows chrome-linux64 dir. Good.
Bash
Find playwright module install location
Think
playwright is available at apps/web/node_modules (with .bin/playwright). So I can run a node script from within apps/web context, or simply use `node -e` requiring playwright from the apps/web node_modules path.
Bash
Create probe tool directory
Write
tools/dsh-web-probe/probe.cjs
Bash
Run browser probe against web GUI
Think
The GUI does render: shows a session list including a session titled "修复Web界面消息显示" (Fix Web interface message display) with "1分钟" — that's this current conversation session! And the right side shows "详情 / 点击消息流中的工具行查看详情 / 命令行走起：cd /home/openclaw/workspa..."

The GUI renders (session list visible). Let me look at the screenshot to see what the main conversation area shows.

失败
Tool call
Error: cannot read "/home/openclaw/workspace/AgentSoftware/tools/dsh-web-probe/shot.png" as an image: model "deepseek-v4-flash" does not declare image input; switch to an image-capable model to read images
Think
No image input available in this model. I'll have to rely on the DOM. Let me extend the probe to dump the full innerText plus a structured outline of main areas (roles/classes). Better: dump text content of the main conversation area container. Let me query DOM: find elements whose class contains "conversation" / "message", list of role attributes, etc.
失败
Tool call
Error: describe-image: baseURL must be an absolute http(s) URL
Think
describe_image is misconfigured in this environment. OK, rely on DOM text. Let me improve the probe to dump a rich text outline: all headings, buttons, and elements matching message-like classes, plus full innerText (maybe 60k) and the DOM structure of main areas. Also let me dump innerText fully without truncation into a file, then read parts.
Write
tools/dsh-web-probe/probe2.cjs
Bash
Run richer DOM probe
Think
Wait — TEXT LENGTH 491 and outline length 31. Very different from the previous run, which had rich content (session list etc.). Now the page seems mostly empty — likely showing a "loading"/empty state or an onboarding screen? Or maybe the page is showing a welcome screen without connecting because the "session" state requires pairing?
Bash
Show second probe body text and outline
Think
OK — interesting! The outline shows that:

The GUI connects fine but the conversation stream is empty. Let me capture actual API traffic (request bodies + responses) and, in parallel, read the client conversation code.

Write
tools/dsh-web-probe/probe3.cjs
Bash
Capture API traffic from GUI
Bash
List ui-conversation sources
Think
Probe 3 recorded only 66 chars → my capture didn't record most requests because resourceType filtering? XHR/fetch requests — Playwright: postData for request... Maybe many requests occurred before listener attach? No, goto happens after listeners. But network events fire after attach. Hmm, maybe the responses couldn't be read because they were already completed (playwright buffers... should work). Or maybe traffic uses POST with content-type that's not json and response reading failed... Wait only "written 66 chars" and no session.history entries, yet the previous probe showed session.history request. Perhaps the SPA was served from cache and requests fired before page listeners attached? No...
Bash
View captured traffic file
Think
Very informative. Key insight:

The picture is getting clearer: the GUI opened a brand-new blank session session-d91d0e8d… (only 3 bootstrap events) while the real conversation lives in session-769883e8… (my session, 21 steps). Let me query the history API directly for my session and dump the raw session log.

Bash
Query history for the real agent session
Bash
Stream-decompress session JSONL
Think
Both succeeded. history-7698.json is 3.6MB (contains events for my session — big). session-7698.jsonl 688KB decompressed.
Bash
Summarize session JSONL event types
Think
The session JSONL (the raw log) contains rich events: reasoning-chunks (249!), assistant/chunk, tool-call-chunks, tool/call, tool/result, step/start, assistant/message, text-chunks, user/message... This is the agent's live record. The JSONL lines appear WITHOUT seq wrapper for some lines (e.g. line1 "session" type has no seq? It has createdAt etc.), but permission/preset has seq. So the JSONL is the packed raw event stream (that's the "session log" the host appends to) — it's the same data that the GUI's session.history should serve (replay from storage).

The session log has rich event types (reasoning-chunks, tool/call, tool/result, assistant/message…). Now I need to understand the client-side rendering path and session-selection logic. Let me explore the source tree and inspect history payload shape in parallel.

Bash
List ui-conversation source files
Bash
Summarize history payload for real session
Think
Interesting: the history for session 769883e8 has **17781 events**, of which 17629 are `assistant/chunk` — that's a lot. Hmm — my session only has steps 21, llm decode 13185 tokens... but 17629 assistant/chunk events? These chunks are probably per-character/per-fragment deltas accumulated over... hmm wait, actually my session's own summary shows decodeTokens 13185 and the session log had 216 assistant/chunk lines total (the raw JSONL only contains 729 lines but the replay/history contains 17781 events, 17629 of which are assistant/chunk!). That's weird: history replay is producing per-*fragment* chunks with duplicates? Wait — maybe the session.history endpoint expands "packed" runs... Hmm, actually 17629 assistant/chunk — the JSONL contains 216 assistant/chunk raw lines (probably each line is one "delta pack" that expands into many events?).
Bash
Convert timestamps to local time
Bash
Read conversation apply.ts head
Think
Time conversion shows:

Timestamps confirm the blank session was created by my own probe page loads (fresh browser state), not by the user. The real question is rendering. Let me trace which event types the client actually consumes.

Bash
Find client code referencing event types
Bash
Find conversationEvents implementation
Think
The runtime client "sessions" assembly handles conversation event application. Notably the fixtures/tests (connection/src/client/fixture.ts, runtime tests conversation-assembler.client.spec.ts) might encode the expected shape. The critical question remains: what exactly is broken in the user's GUI?

Let me drive the real session into view: click the running conversation in the sidebar and inspect what renders.

Write
tools/dsh-web-probe/probe4.cjs
运行中
Bash
Run session-click probe
Deep diving...
5分59秒
任务
1 进行中 · 3 待处理
Workspace Write
DeepSeek-V4-Flash
High
1 轮 · 29 步| LLM 3m49s · 工具调用 1m43s| 首 token 平均 0.9s · 139 tok/s| 缓存命中 96%| 输入 1.1M tok · 输出 28.1K tok
详情
点击消息流中的工具行查看详情
正在使用 bash