# ── Java / Maven ──────────────────────────────────────────
target/
*.class
*.jar
!.mvn/wrapper/maven-wrapper.jar
.idea/
*.iml

# ── 运行时数据 (与 Python 版一致, 不入库) ────────────────
data/journals/
data/notes/
# 运行时数据: 角色个人电脑目录 (LLM 生成产物) + 演示日志 + 技能库, 不入库
data/computers/
data/main_run.log
data/skills/
data/todos/
data/drive/
data/mail/
data/state.json
# 镜像构建进程间锁 (fcntl 文件锁, 运行时产物)
data/.maf-base-image.lock

.DS_Store
*.egg-info/
dist/
build/
.venv/
venv/
.vscode/
# Hermes 内部规划目录 (plan skill 产物, 不是项目文件)
.hermes/
# 敏感凭据 (SSH 私钥/API key 等) — 曾误入提交历史, 禁止再跟踪
*_key
*_key.pub
*.pem
*.key
# 根目录杂项脚本 (库版本检查等, 非项目代码)
library_checker.py

# 本地工具链 (Maven), 不入库
.tools/
.scratch/

# ── Python 残留 (原版遗留) ─────────────────────────────
__pycache__/
*.pyc
*.pyo

# ── 本地 Maven 仓库 (沙箱只读 ~/.m2 时的可写替代) ────────
.m2w/
