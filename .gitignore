# ── Java / Maven ──────────────────────────────────────────
target/
*.class
*.jar
!.mvn/wrapper/maven-wrapper.jar
.idea/
*.iml

# ── Runtime data (generated by a simulation run) ──────────
data/journals/
data/notes/
data/computers/
data/main_run.log
data/skills/
data/todos/
data/drive/
data/mail/
data/state.json
data/.maf-base-image.lock

.DS_Store
dist/
build/
.venv/
venv/
.vscode/

# Sensitive credentials (SSH private keys / API keys)
*_key
*_key.pub
*.pem
*.key

# Local toolchain / scratch
.tools/
.scratch/

# ── Local Maven repository (used when ~/.m2 is read-only) ──
.m2w/
.m2local/
.m2-local/
