# ── Java / Maven ──────────────────────────────────────────
target/
*.class
*.jar
!.mvn/wrapper/maven-wrapper.jar
.idea/
*.iml

# ── Runtime data (same as the Python version, not committed) ──
data/journals/
data/notes/
# Runtime data: role personal computer dirs (LLM-generated artifacts) + demo logs + skill libraries, not committed
data/computers/
data/main_run.log
data/skills/
data/todos/
data/drive/
data/mail/
data/state.json
# Image-build inter-process lock (fcntl file lock, runtime artifact)
data/.maf-base-image.lock

.DS_Store
*.egg-info/
dist/
build/
.venv/
venv/
.vscode/
# Hermes internal planning dir (plan skill artifacts, not project files)
.hermes/
# Sensitive credentials (SSH private keys/API keys etc.) — once committed by accident, now forbidden
*_key
*_key.pub
*.pem
*.key
# Root-level misc scripts (library version checks etc., not project code)
library_checker.py

# Local toolchain (Maven), not committed
.tools/
.scratch/

# ── Python leftovers (from the original version) ────────
__pycache__/
*.pyc
*.pyo

# ── Local Maven repo (writable replacement when ~/.m2 is read-only in sandbox) ──
.m2w/
.m2local/
